# Timestamp model

There is work on flow-matching models that are timestamp independent. This is possible for instantaneous velocity, but does not make sense for mean-flow and other flow-map models that need to calculate average velocities based on a timestamp. This work tries to teach a model to guess the timestamp of a noised image.
